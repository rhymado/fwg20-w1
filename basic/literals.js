// digunakan untuk
// 1. penempelan ekspresi
// 1.1 variabel
// 1.2 operasi kode
// 2. penulisan multi line

// literal dibungkus menggunakan backtick (``)
const a = 20;
const b = 15;

console.log(
  `nilai a = ${a}
nilai b = ${b}
a + b = ${a + b}
a - b = ${a - b}`
);
